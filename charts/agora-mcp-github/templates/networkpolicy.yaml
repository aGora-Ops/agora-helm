apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "agora-mcp-github.fullname" . }}-netpol
  namespace: agora
spec:
  podSelector:
    matchLabels:
      {{- include "agora-mcp-github.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: agora
      ports:
        - port: {{ .Values.service.port }}
  egress:
    - {}
